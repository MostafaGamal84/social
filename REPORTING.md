# إنشاء تقارير العروض التقديمية المدعومة بالذكاء الاصطناعي

يوضح هذا الدليل كيفية استخدام نظام الرصد الإعلامي لإنشاء تقارير PowerPoint اعتماداً على القالب الجاهز ومخرجات الذكاء الاصطناعي.

## تجهيز القالب

1. أنشئ المجلد `templates/` في جذر المشروع إذا لم يكن موجوداً.
2. أضف ملف القالب `report_template.pptx` إلى هذا المجلد (لا يتم تضمين الملف في المستودع).
3. احرص على أن يحتوي القالب على العناصر النصية التالية ليتم استبدالها تلقائياً:
   - `{{REPORT_TITLE}}`
   - `{{REPORT_SUMMARY}}`
   - `{{GENERATED_ON}}`
   - `{{SECTION_{n}_TITLE}}`
   - `{{SECTION_{n}_BODY}}`
   - `{{SECTION_{n}_BULLETS}}`
   - `{{SECTION_{n}_BULLET_{m}}}` (حيث n يبدأ من 1، و m من 1 وحتى العدد الأقصى المحدد في الإعدادات).

## إعدادات الخادم

تتم قراءة إعدادات التقارير من القسم `Reporting` في ملفات `appsettings*.json`:

```json
"Reporting": {
  "TemplatePath": "templates/report_template.pptx",
  "DefaultFileName": "media-report.pptx",
  "MaxBulletPlaceholders": 5
}
```

يمكن تعديل المسار أو اسم الملف الافتراضي وفقاً لبيئة النشر. يستخدم الخادم خدمة OpenAI (أو يستعيض عنها بمحتوى بديل عند غياب المفتاح) لتوليد المحتوى قبل حقنه في القالب.

## واجهة برمجة التطبيقات

- **المسار:** `POST /api/report/generate`
- **المدخلات:** كائن JSON يحوي حقولاً اختيارية `prompt`, `reportTitle`, `summary`, `sections`.
- **المخرجات:** ملف PowerPoint (`application/vnd.openxmlformats-officedocument.presentationml.presentation`).

يمكن إرسال نص حر في `prompt` ليقوم الخادم بتوليد الأقسام تلقائياً، أو تمرير الأقسام يدوياً عبر الحقل `sections`.

## الواجهة الأمامية

- تمت إضافة زر “إنشاء تقرير PowerPoint” إلى شريط الأدوات الرئيسي لتجهيز تقرير بناءً على البيانات والمرشحات الحالية في لوحة المتابعة.
- يوفر مساعد الدردشة زر “تقرير المحادثة” الذي يرسل سياق الحوار إلى واجهة التقارير ويبدأ تنزيل الملف الناتج.
- يتم التعامل مع حالات الانتظار والأخطاء برسائل واضحة للمستخدم داخل كل من الواجهة الرئيسية ونافذة الدردشة.

## الاختبارات

- يحتوي المشروع على اختبارات وحدة (`API.Tests/ReportGenerationServiceTests.cs`) تتحقق من استبدال القيم داخل شرائح القالب.
- تم إضافة اختبار للوحدة الأمامية (`design/src/app/services/report.service.spec.ts`) لضمان أن خدمة التقارير تستدعي واجهة البرمجة بالشكل الصحيح.

باتباع الخطوات السابقة يمكن للمستخدمين توليد تقارير عروض تقديمية جاهزة للمشاركة مباشرة من لوحة المراقبة أو من خلال المساعد التفاعلي.
